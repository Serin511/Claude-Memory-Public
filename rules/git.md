## Git Rules

IMPORTANT: Never run git commit unless I explicitly request it. This rule applies at all times without exception.
IMPORTANT: Similarly, never run git push unless I explicitly request it. This rule applies at all times without exception.

**Exception**: If the `/push` command (`commands/push.md`) is directly invoked, both commit and push are considered explicitly requested.
